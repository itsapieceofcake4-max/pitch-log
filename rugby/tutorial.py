"""
rugby/tutorial.py
=================
アプリ内ヘルプの文面。UI から分離して、文言だけを直せるようにしてある。

- `TAB_HELP`  … 各タブ冒頭の折りたたみヘルプ
- `STEPS`     … 「使い方」タブのチュートリアル本体
- `TROUBLE`   … よくある症状と対処
- `GLOSSARY`  … 用語集
"""

from __future__ import annotations


# ── 全体の流れ ────────────────────────────────────────────────────────────────

OVERVIEW = """
このアプリは **上空から撮った映像だけ** から、選手の位置を時系列の数値
（トラッキングデータ）に起こすためのものです。GPS も専用機材も要りません。

```
  動画  ──▶  ①どこがピッチか  ──▶  ②動く点を拾う  ──▶  ③点を追い続ける  ──▶  CSV
              教える（②タブ）        （③タブで自動）      （③タブで自動）
```

やることは実質 **2 つだけ** です。

1. **ピッチの範囲を教える**（②キャリブレーション）— 最初の 1 回だけ
2. **解析したい 30 秒を選ぶ**（③区間解析）— シーンごとに

あとは自動で処理され、④〜⑦で結果を見たり書き出したりします。
"""


# ── 各タブ冒頭のヘルプ ────────────────────────────────────────────────────────

TAB_HELP: dict[str, str] = {
    "video": """
**このタブですること**：解析したい動画を読み込みます。

- **この PC で動かしている場合、長い試合映像のままで構いません。**
  解析は③で指定した 30 秒だけ行うので、事前に切り出す必要はありません。
- **クラウド（URL）で使っている場合は、先に切り出しが必要です。**
  80 分の試合は 1080p で 2〜6GB あり、そのままではアップロードできません。
  長い映像を読み込むと「この試合から切り出す」欄が出るので、そこで必要な
  40 秒ほどを切り出してください（数十 MB になります）。手元の PC で
  切り出してから、そのファイルをクラウドへ上げる流れになります。
- 向いている映像：**上空・俯瞰から撮ったもの**。三脚などで固定されていると
  最も安定します。ドローンや手持ちでも、③で「手ブレ補正」を有効にすれば使えます。
- 解像度は選手が 10 px 以上に写っていれば実用になります（フル HD で
  ピッチ全体を収めた場合、選手はだいたい 15〜25 px です）。
- パスを直接入力するのが確実です。アップロードは 200MB までの制限があります。
""",
    "calibration": """
**このタブですること**：映像のどこがピッチなのかを教えます。**最初の 1 回だけ**です。

**なぜ必要？** 映像はピクセルの集まりでしかなく、そのままでは「画面の右に 50 px」
としか分かりません。ピッチの実寸（100m × 70m）と対応づけて初めて
「ゴールから 23m の地点」という意味のある座標になります。

**手順（かんたんモード）**
1. ピッチ全体がよく見えるフレームを選ぶ
2. **ピッチの外周に沿って 4 本の線を引く**（左右 2 本＋上下 2 本で囲む）
3. 「囲んだ範囲」を選ぶ — ここだけ注意（下記）
4. ボタンを押し、**黄色い枠が実際のラインと重なっているか目で確認**

**「囲んだ範囲」の選び方**：ラグビーはインゴール（ゴールラインの外側）があるため、
どこを囲んだかで縮尺が変わります。
- 一番外側（デッドボールライン）を囲んだ → **デッドボールラインまで**
- トライラインどうしを囲んだ → **ゴールライン間**

判断に迷ったら両方試して、**22m ラインやハーフウェイの位置が合うほう**が正解です。

**線を引く順番・向きは何でも構いません。** どの線がどのラインかは自動判別します。

> ⚠ この方式では表示される誤差の数値は必ず 0 になります（4 点はピッチの
> 対応を一意に決めるため、常に完全一致するからです）。**数値ではなく
> 黄色い枠の見た目で判断してください。**
""",
    "analyze": """
**このタブですること**：解析する 30 秒を選んで実行します。

**なぜ 30 秒？** 因果分析（GSA）が 1 シーン単位で行うためです。トライの直前や
決定的な場面など、見たい場面を選んでください。長くすることもできます。

**設定の意味**（迷ったら既定のままで構いません）

| 設定 | どういうとき変える |
|---|---|
| **検出方式** | 既定の `bgsub`（背景差分）は固定カメラ向き。動かない選手は苦手。`yolo` は静止した選手も拾えるが `ultralytics` の導入が必要 |
| **手ブレ補正** | **ドローン・手持ち撮影なら必ずオン**。三脚固定なら不要。処理時間が約2倍になる |
| **処理間隔** | 2 にすると半分のフレームだけ処理して約2倍速くなる。試し打ちのときに便利 |
| **選手の想定サイズ** | 検出数が明らかに合わないときに調整。俯瞰で1人が占める大きさ（m） |
| **最短トラック長** | 実在しない短いトラックが多いときに伸ばす |
| **観測ノイズ / 想定加速度** | 位置がガタつくなら観測ノイズを大きく、動きに追従しないなら想定加速度を大きく |

**手ブレ補正は効果が非常に大きい**設定です。カメラが揺れている映像で切ったまま
解析すると、追跡が破綻して実在しないトラックが大量に生まれます
（検証では 34 本が 676 本に増えました）。
""",
    "results": """
**このタブですること**：追跡結果を目で確認し、追跡 ID に背番号を割り当てます。

**まず見るところ**：「選手トラック」の数が「想定人数」と近いか。
大きく上回っている場合は誤検出が混じっています → ③の「最短トラック長」を
伸ばすか「選手の想定サイズ」を調整して再解析してください。

**カバー率**：そのトラックが解析区間の何割に映っていたか。1.0 に近いほど
最初から最後まで追えています。低いものは途中で見失ったか、誤検出です。

**背番号の割当**：表でチームと背番号を入れて「割当を反映」を押すと、CSV の
列名がその番号になります（例：`Home_P10_X`）。空欄のままでも出力できます
（出現の長い順に P1, P2… と自動採番されます）。

**ボロノイ図**：各選手が「最も早く到達できる範囲」でピッチを塗り分けたものです。
相手の領域の間に自チームの領域が食い込んでいれば、そこが安全にパスを受けられる
スペースです。守備側の裏に相手の広い領域があれば、そこが危険地帯です。
""",
    "physical": """
**このタブですること**：走行距離・速度・スプリント回数を算出します。

**指標の意味**

| 指標 | 内容 |
|---|---|
| 総走行距離 | 静止判定を除いた実移動の累計 |
| 最高速度 | 平滑化後の速度の最大値 |
| 平均速度 | **停止時間を除いた**平均 |
| スプリント回数 | 閾値超えが指定秒数続いた回数 |
| 断絶(discontinuities) | 追跡が飛んだ回数。**信頼度の目安** |

**⚠ 断絶の数を必ず確認してください。** 値が大きい選手は、追跡が途中で別の選手に
乗り移った可能性があり、走行距離や最高速度が過大になりがちです。

**スプリント閾値**：既定の 24 km/h はサッカーの一般基準です。ラグビーでは
ポジションによって 20 km/h 前後で運用することもあるので、チームの基準に
合わせて変えてください。

**これは 30 秒間の値です。** 試合全体の走行距離ではありません。
""",
    "animation": """
**このタブですること**：2D のタクティカルマップ動画（MP4）を書き出します。

**2 つの使い道があります。**
1. **確認用** — 抽出した座標が正しいか目で見て確かめる。元映像と見比べて、
   選手の動きが一致していれば成功です
2. **分析用** — 戦術ミーティングでそのまま使える 2D マップ映像として

**軌跡（テール）**：過去どれだけの動きを線で残すか。25fps なら 30 フレームで
約 1 秒ぶんです。動線の方向が見えるようになります。

**空間支配率の推移**：ボロノイ図で計算した各チームの支配面積の割合です。
押し込んでいる時間帯、押し返された瞬間が読み取れます。
""",
    "export": """
**このタブですること**：解析結果を CSV で書き出します。

**3 つの形式があります。使い分けは以下のとおりです。**

| 形式 | 中身 | 使いどころ |
|---|---|---|
| **ワイド形式** | 1 行 = 1 フレーム、列が選手ごと | 主たる納品物。既存 Pitch Log にそのまま載る |
| **長形式** | 1 行 = 1 選手 1 フレーム | Excel のピボットや自前の分析 |
| **仕様書スキーマ** | 統合仕様書の定義どおり | 他システムとの連携 |

**座標系の選択**
- **メートル** — 実寸。距離や速度を扱う分析向き
- **正規化 0–1** — 既存 Pitch Log（Export_GSA）と互換

列名は既存の規約（`Home_P1_X` / `Away_P3_Y` / `Ball_X`）に揃えてあるので、
そのまま既存の可視化・特徴量付与パイプラインに流せます。
""",
}


# ── チュートリアル本体 ────────────────────────────────────────────────────────

STEPS: list[dict] = [
    {
        "title": "① 動画を読み込む",
        "body": """
動画ファイルのパスを入力します（アップロードでも可）。

**長い試合映像のままで構いません。** 解析するのは③で指定した区間だけです。

読み込むと長さ・フレームレート・解像度が表示されます。スライダーを動かして
中身を確認できます。
""",
        "tip": "ピッチ全体が写っていて、選手が重なっていないフレームを見つけておくと "
               "次のキャリブレーションが楽になります。",
    },
    {
        "title": "② ピッチの範囲を教える（最重要・1回だけ）",
        "body": """
**ピッチの外周に沿って 4 本の線を引いて囲みます。** 左右のライン 2 本と、
上下のライン 2 本です。

- 線は**ドラッグ**で引きます
- **順番も向きも自由**です。どの線がどのラインかは自動で判別します
- 引き直したいときは、キャンバス左下のゴミ箱で全消去できます

次に「囲んだ範囲」を選びます。ラグビーはインゴールの分だけ縮尺が変わるため、
ここだけ指定が必要です。

最後にボタンを押すと、復元したピッチ枠が黄色い線で重ねて表示されます。
""",
        "tip": "黄色い枠が実際のラインとぴったり重なれば成功です。22m ラインや "
               "ハーフウェイの位置がずれていたら「囲んだ範囲」の選択を、"
               "左右・上下が逆なら反転チェックを切り替えてください。",
    },
    {
        "title": "③ 解析する 30 秒を選んで実行",
        "body": """
開始位置のスライダーで見たい場面を選び、「この区間を解析」を押します。

**設定は既定のままで構いません**が、1 つだけ判断が要ります。

> **カメラが動く映像（ドローン・手持ち）なら「手ブレ補正」を必ずオンに。**

処理時間の目安は 30 秒の映像で 45〜90 秒程度（手ブレ補正ありなら約 2 倍）です。
""",
        "tip": "まず「処理間隔」を 2 にして試し打ちすると、半分の時間で結果の "
               "当たりを付けられます。良さそうなら 1 に戻して本番実行してください。",
    },
    {
        "title": "④ 結果を確認して背番号を振る",
        "body": """
2D マップで選手の動きを再生し、元映像と見比べて正しく追えているか確認します。

「選手トラック」の数が想定人数と近ければ良好です。多すぎる場合は誤検出が
混じっているので、③に戻って調整します。

表でチームと背番号を入力して「割当を反映」を押すと、以降の出力に反映されます。
""",
        "tip": "背番号は必須ではありません。空欄なら出現の長い順に P1, P2… と "
               "自動採番されます。まず全体を確認してから、必要な選手だけ振るのが効率的です。",
    },
    {
        "title": "⑤〜⑦ 分析して書き出す",
        "body": """
- **⑤フィジカル** — 走行距離・最高速度・スプリント回数。閾値は画面上で変えられます
- **⑥2Dアニメーション** — タクティカルマップ動画（MP4）の書き出し
- **⑦CSV出力** — 座標データの書き出し

いずれも③の解析結果をもとに計算されるので、③をやり直すと更新されます。
""",
        "tip": "⑥で書き出した動画を元映像と並べて見ると、抽出精度をチームに"
               "説明しやすくなります。",
    },
]


# ── よくある症状と対処 ────────────────────────────────────────────────────────

TROUBLE: list[dict] = [
    {
        "symptom": "選手がほとんど検出されない",
        "cause": "検出の大きさフィルタが実際と合っていないか、選手が動いていない",
        "fix": "③の「選手の想定サイズ」を変えてみてください。"
               "背景差分は**動かない選手を検出できません**。"
               "静止が多い映像では検出方式を `yolo` に切り替えてください"
               "（`pip install ultralytics` が必要です）。",
    },
    {
        "symptom": "トラック数が実際の人数よりずっと多い",
        "cause": "短命な誤検出が混じっている",
        "fix": "③の「最短トラック長」を 1.5〜2 秒に伸ばしてください。"
               "それでも減らない場合は「選手の想定サイズ」が小さすぎて、"
               "1 人が複数に分割されている可能性があります。",
    },
    {
        "symptom": "座標が全体的にずれている／ピッチ外に出る",
        "cause": "キャリブレーションのずれ、または手ブレ",
        "fix": "②に戻り、黄色い枠が実際のラインと重なっているか確認してください。"
               "枠は合っているのに座標がずれる場合は、カメラが揺れています。"
               "③の「手ブレ補正」をオンにしてください。",
    },
    {
        "symptom": "選手が入れ替わる（ID が飛ぶ）",
        "cause": "重なりの後で取り違えている",
        "fix": "④の「カバー率」が低いトラックを確認してください。"
               "③の「観測ノイズ」を小さく、「想定加速度」を大きくすると"
               "追従性が上がります。手ブレ補正の切り忘れも典型的な原因です。",
    },
    {
        "symptom": "最高速度が明らかにおかしい（40km/h など）",
        "cause": "追跡が別の選手に飛んでいる",
        "fix": "⑤の「断絶(discontinuities)」列を見てください。"
               "値が大きい選手は数値が信用できません。"
               "追跡を安定させるほうが先です（上の項目を参照）。",
    },
    {
        "symptom": "ボールが検出されない・位置がおかしい",
        "cause": "上空映像のボール検出は原理的に難しい",
        "fix": "**既定では無効にしてあります。** ボールは十数ピクセルしかなく、"
               "芝のノイズと区別できないためです（検証では誤差 15m 超）。"
               "必要な場合は検出方式 `yolo` を高解像度映像に対して使うか、"
               "手動での記録を検討してください。",
    },
]


# ── 用語集 ────────────────────────────────────────────────────────────────────

GLOSSARY: list[tuple[str, str]] = [
    ("キャリブレーション",
     "映像のピクセルと、ピッチの実寸（メートル）を対応づける作業。"
     "これをしないと「画面上の位置」しか分からず、距離も速度も計算できません。"),
    ("ホモグラフィ",
     "平面から平面への変換ルール。ピッチは平面なので、映像に写った像との対応は"
     "3×3 の行列ひとつで表せます。②で求めているのがこれです。"),
    ("トラッキング / track_id",
     "フレームをまたいで「これは同じ選手だ」と紐づけ続けること。"
     "その通し番号が track_id です。背番号とは別物で、映像ごとに振り直されます。"),
    ("オクルージョン（重なり）",
     "選手が重なって隠れること。追跡が最も失敗しやすい瞬間です。"
     "本アプリは速度の物理的な上限・動きの予測・服の色・チームの4つで対処しています。"),
    ("カバー率",
     "そのトラックが解析区間の何割に映っていたか。1.0 なら最初から最後まで"
     "追い切れています。"),
    ("断絶（discontinuities）",
     "追跡が物理的にありえない距離を飛んだ回数。ID の取り違えの目安になります。"
     "多い選手のフィジカル数値は過大になりがちです。"),
    ("ジッター",
     "検出枠の微小な揺れ。選手が止まっていても数ピクセル揺れるため、"
     "そのまま計算すると「常に動いている」ことになり距離や速度が跳ね上がります。"),
    ("ボロノイ図",
     "各選手が最も早く到達できる範囲でピッチを塗り分けた図。"
     "どちらのチームがどれだけスペースを支配しているかが一目で分かります。"),
    ("再投影誤差",
     "キャリブレーションのずれを表す数値。ただし**4点ちょうどで求めた場合は"
     "必ず 0 になり、品質の指標になりません**。見た目で確認してください。"),
]
