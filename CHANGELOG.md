# Changelog — Pitch Log

変更履歴。新しいものを上に追記していく。日付は実装日（git log ベース）。
形式: `- 内容 (コミット短縮ハッシュ)`

> 運用ルール：機能を追加・変更したら、push 前にこのファイルへ1行追記する。

---

## 2026-08-07

- **app_25（v25）新規：ラグビー上空映像トラッキング**。トラッキングデータが存在しない競技向けに「映像→座標」の入口を新規実装。`rugby/` パッケージ（pitch_model / calibration / detection / tracking / pipeline / export）＋ Streamlit UI（①動画 ②キャリブレーション ③区間解析 ④結果・背番号 ⑤CSV出力）。ピッチラインの交点を4点以上クリックしてホモグラフィを推定し、長い試合映像から任意の30秒だけを解析。出力は縦=時間/横=選手のワイドCSV（列名は既存 Export_GSA 規約に準拠）。
  - 追跡はピッチ座標(m)で実施 — 速度ゲート(11m/s)と選手サイズが画面内位置によらず一定になる。
  - 遮蔽対策4段：物理速度ゲート／カルマン予測／HSVヒストグラム再同定／チーム拘束。加えて **マッチングカスケード**（DeepSORT方式）で、見失ったトラックが全体コスト最小化の名目で正しいトラックから検出を奪う問題を解消。
  - 密集塊の分割閾値を**実測面積の中央値から自己較正**。幾何推定のままだと2人の重なりが「1人」として通過し、交錯のたびにIDが分裂していた。
  - 速度は位置と分離し、出力位置の平滑微分（5フレーム窓）で算出。※窓はGSAの因果ラグより短く保つこと。
  - 検証: `rugby/make_synthetic.py` で正解座標つき合成映像を生成、`rugby/validate.py` で測定。**位置誤差 中央値0.086m／検出率98.9%／単一ID被覆率99.3%／1選手あたり1.07トラック**。
  - ⚠ **ボール検出は既定オフ**。上空映像のボールは数ピクセルで芝ノイズと区別できず、検証で位置誤差 中央値15m超と実用にならなかったため。誤座標で下流分析を壊さないことを優先。YOLOバックエンドか手動アノテーションが現実的な選択肢。
  - 詳細は `docs/RUGBY_TRACKING.md`。
- **v25 拡張：ALLCLIP 統合仕様書（モジュール2〜5）を実装**。原本は `docs/Allclip_Tracking_Specs.md`。UI は全7タブに拡張（⑤フィジカル ⑥2Dアニメーション を追加）。
  - **モジュール2** `rugby/animate.py`：2Dタクティカルマップ MP4 生成。軌跡（テール）・チーム色分け・ボロノイ層・時刻表示。matplotlib ではなく OpenCV 描画（背景を使い回せるため1桁高速）。CLI: `python -m rugby.animate`。
  - **モジュール3** チーム識別を強化：ジャージ色抽出を「枠の上部60%クロップ＋芝色相のHSVマスク」に変更。枠全体の平均色だと背景の緑やパンツに引きずられて2チームが分離しない。
  - **モジュール4** `rugby/physical.py`：走行距離／最高速度／スプリント回数。ジッター対策4段（座標平滑化→速度平滑化→デッドバンド→継続時間条件）に加え、**ID取り違えと欠測でトラックを区間分割**してから微分。これが無いと最高速度が 37.0km/h（正解15.5）に化けた → 20.2km/h に改善。走行距離は正解19.5m対推定18.5m（誤差5%）。`discontinuities` 列で信頼度を可視化。
  - **モジュール5** `rugby/voronoi.py`：空間支配の可視化。無限領域は**母点をピッチ外へ鏡像反転**して有界化し、shapely でピッチ矩形とクリップ。検証でセル合計面積＝ピッチ面積（7000m²）・支配率合計1.0000・はみ出し0を確認。支配率の時系列も出力。
  - サッカー規格（105×68・センターサークル・PA/GA）とサッカー用ランドマーク27点を追加。仕様書がサッカー前提のため。
  - YOLO の接地点を俯角から自動選択（真俯瞰は枠中心／斜めは底辺中央）。真俯瞰で底辺中央を使うと体半分ぶん系統的にずれるため。
  - 出力に仕様書スキーマ（`frame_idx` / `timestamp` / `track_id` / `x_pitch` / `y_pitch` / `team_id`）を追加。
- **v25 デプロイ対応と長尺映像の切り出し機能**。`docs/DEPLOY.md` に会社PCインストール／限定公開／一般公開の3案を比較して記載。
  - `rugby/clip.py` を追加。長い試合映像から解析したい時間帯だけを切り出す。ffmpeg があれば `-c copy` で再エンコードせず切り出し（**実測: 10分839MBの映像から30秒を0.33秒・42MB＝元の5%**）、無ければ OpenCV へフォールバック。①タブに UI を統合（2分超の映像を読み込むと自動で表示）。CLI は `python -m rugby.clip`。
  - **背景**: ラグビーの試合は80分＋で1080pなら2〜6GB。Streamlit のアップロードは全体をメモリに載せるためクラウドでは通らない。解析に使うのは30秒だけなので、先に切り出せば数十MBに収まる。
  - `requirements.txt` を `opencv-python` → **`opencv-python-headless`** に変更。GUI機能（imshow等）を一切使っていないため headless で足り、サーバ側に libGL 等の apt パッケージが不要になる。動画の読み書きが headless でも動作することを確認済み。
  - 🔴 **公開リポジトリの保護**: リポジトリが既に public であることを確認したうえで、`.gitignore` に NDA データ由来の成果物（`docs/Pitch_Log_RealData_*.pptx`）・競合分析（`competitor_map.pptx`/`competitors.xlsx`）・実映像・生成CSVを追加。追跡済みファイルと履歴に認証情報の漏洩がないことも監査済み（SoccerNet パスワードは環境変数化されており痕跡なし）。
- **v25 デザインを「Modern Turf（上質な芝）」に刷新**。ベタ塗りの緑ではなく、深いエメラルド基調＋刈り込みストライプで質感を出す方向へ全面変更。
  - **芝のストライプを実装**。`theme.turf_stripes()` の定義を Plotly の2Dマップと書き出しMP4が共有し、8mごとの濃淡（`#0E3A27`/`#134A32`）を描く。ピッチ外は暗色に落として面の輪郭を締める。
  - **アクセントを3役に限定**：ゴールド `#FBBF24`（操作）／シアン `#22D3EE`（チームA）／コーラル `#FB7185`（チームB）。Tailwind系のフラットな色域から、緑背景でも濁らないものを選定。彩度の高い原色は不使用。
  - **視線導線**：節見出しを `theme.section()` に統一し、左に短いゴールドのアクセントバーを付けて視線の起点を作る（アプリ内15か所を置換）。8pxグリッドで余白を統一。
  - **立体感**：選手ドットにドロップシャドウ（Plotlyはオフセット黒マーカー層、OpenCVは合成）を追加しピッチから浮かせる。ボロノイは alpha 0.35→0.25 に下げ芝が透けるようにし、境界を1pxの極細に。
  - **書体**：Inter を Google Fonts から読み込み（未取得時はシステムフォントへフォールバック）。数値は `tabular-nums` で桁揃え。
  - ピッチラインは透過白（alpha 0.72〜0.78）でシャープだが硬すぎない印象に。
  - 検証: 1440px / 1024px 両方でヘッダー70px・1行を維持し横スクロールなしを確認。Inter の読み込み成否も `document.fonts.check` で確認済み。
- **v25 UI を暗色のスポーツ分析ダッシュボードに刷新**。デザインシステムを `rugby/theme.py` に集約（パレット19色・CSS・Plotlyレイアウト・ヘッダー/進行状況のコンポーネント）。`.streamlit/config.toml` で下地を暗色にし初回描画の白いちらつきを防止。
  - **配色は1か所で管理**。`COLORS` を Streamlit の CSS・Plotly のグラフ・書き出す MP4 の3か所すべてが参照するため、色を変えれば画面も動画も同時に変わる。
  - アクセントは発光する緑 `#00E5A0`（ピッチの連想＋データの躍動）。面は `#080B11`〜`#161E2A` の暗色3段で奥行きを作り、データを最も明るく見せる。
  - **数値は等幅フォント**（桁が揃い比較しやすい）。日本語は Yu Gothic UI / Hiragino Sans を含むフォントスタックを指定。
  - カスタムヘッダー（ロゴマーク＋進行状況チップ VIDEO/CALIBRATED/TRACKED）、タブの下線アクセント、メトリクスのカード化とホバー、プライマリボタンのグラデーションを追加。サイドバーの進行状況も専用コンポーネント化。
  - 書き出す MP4 にも情報バー（PITCH LOG／時刻／フレーム／検出人数）を追加し、ピッチ外の余白を UI と同じ暗色にしてピッチ面を浮かび上がらせた。
  - Streamlit の内部クラス名は版ごとに変わるため、セレクタは `data-testid` を優先。
- **v25 アプリ内ヘルプ・チュートリアルを追加**。文面は `rugby/tutorial.py` に分離（UIと分けて文言だけ直せるように）。
  - **「📖 使い方」タブ**を新設（全8タブ構成に）。全体の流れ図／手順5ステップ（各ヒント付き）／症状別トラブルシュート6件／用語集9件／精度の目安を掲載。
  - **各タブ冒頭に折りたたみヘルプ**を追加。そのタブで何をするか・設定の意味・迷いやすい点を記載。未閲覧のタブは開いた状態、once見たら畳んだ状態で表示。
  - **サイドバーに進行状況**（①動画→②キャリブ→③解析のチェックリスト）と「次にやること」の案内を追加。
- **v25 キャリブレーションに「4本の線で囲む」方式を追加（既定）**。ラインの名前指定を廃し、ピッチ外周を4本引くだけで完了。傾きで縦向き2本・横向き2本に振り分け→交点4つ→重心まわりの偏角で環状ソート→隣接辺の向きで回り方を決定、という手順で「左上→右上→右下→左下」を自動確定する。**線を引く順番も向きも不問**（検証済み）。ユーザーが選ぶのは「囲んだ範囲」（デッドボールラインまで／ゴールライン間）と左右・上下の反転のみ。
  - 🔴 実装時のバグ: 画像座標はY軸が下向きのため**多角形の巻き方向の判定が反転**し、符号付き面積で判定すると対角が入れ替わっていた（往復誤差11.6m）。辺の向き（横長か縦長か）で判定する方式へ修正し、ラグビー/サッカー・俯瞰/斜め・インゴール有無の全条件で往復誤差 0.0000m を確認。
  - キャリブレーションは3方式（4本で囲む／ラインを個別に指定／点をクリック）から選択可能に。ピッチが部分的にしか写っていない場合のみ個別指定を使う。
- **v25 キャリブレーションを「線をなぞる」方式に刷新**。交点を1つずつクリックする方式（ドロップダウン2段→クリックの繰り返し）が煩雑だったため、映像上でピッチラインをドラッグしてなぞり、その交点からホモグラフィを求める方式を追加（`calibrate_from_lines` / `PitchLine` / `build_pitch_line_defs`）。従来の点クリック方式もラジオで選択可能。
  - **精度面でも線のほうが有利**: 線は長く取れるため端点の数pxのずれが向きに与える影響が小さく、隅が画面外・遮蔽されていても延長交点として計算できる。縦断3本×横断2本なら6交点が得られRANSACで外れ値を吸収。
  - ⚠ **4点ちょうどでは再投影誤差が必ず0.000になる**（4点はホモグラフィを一意に決めるため常に完全一致）ことをUIとドキュメントに明記。線を1本足すと初めて実際のずれが見える（端点1pxノイズで0.055m、3pxで0.430m）。
  - `streamlit-drawable-canvas` 0.9.3 と Streamlit 1.58 の互換シム `rugby/_canvas_compat.py` を追加。`image_to_url` の移動（`streamlit.elements.image` → `streamlit.elements.lib.image_utils`）と、第2引数の型変更（`width: int` → `LayoutConfig`）の両方に対応。
  - コンポーネントが自身の寸法をStreamlitへ伝えられずiframeが height=0 / width=300 に潰れる問題を、実寸px指定のCSSで回避（%指定だと中のキャンバスと比率がずれ、見た目と座標が食い違うためpx固定）。
  - 検証: fabric.js 4.4.0 実物で Line の JSON 形状を確認し、パーサ `_canvas_lines` が順方向・逆方向ドラッグ・表示縮尺のすべてで正しく端点を復元することを確認。
- **v25 耐性テストと手ブレ補正の基準フレーム修正**。`make_synthetic.py` に `--players`（物体数）・`--motion crossing`（左右から直進して密に交錯させる過酷条件）・`--shake`（手ブレ）を追加し、9条件で測定。
  - 結果: **位置誤差は条件によらず約0.09mで安定**。80物体まで実用的（単一ID被覆 0.983→0.926）。密な交錯はほぼ影響なし（30物体で被覆0.980）。斜め映像はp90誤差がほぼ倍（0.133→0.245m）。
  - 🔴 **手ブレ補正の基準フレームのバグを発見・修正**。基準を「解析窓の先頭フレーム」にしていたため、そのフレーム自体のブレ量ぶん座標が丸ごとオフセットし、6pxのブレで**0.7mの系統誤差**が出ていた。`Calibration.ref_time_sec` を保持してクリックしたフレームへ揃えるよう修正 → 誤差 0.721m → **0.086m**（無風条件と同水準）。
  - 手ブレ補正なしだとトラック数が34→676本・分裂5.67倍と壊滅するため、ドローン/手持ち撮影では必須。処理時間は約2倍。

---

## 2026-06-21

- 競合マップ更新 `docs/make_competitor_map.py` 新規追加。★★★3社（SciSports/impect/SkillCorner）の裏取り済み精密プロファイルを反映。impect→Catapult買収($139M AUD, 2025/10)・SkillCorner→$60M調達(2025/12)・SciSports→Vanad Group買収(2024/11)を記載。`docs/competitor_map.pptx`（ポジショニングマップ+3社プロファイルカード 2スライド）・`docs/competitors.xlsx`（11社×15列+軸定義シート）を生成。

---

## 2026-06-14

- app_22: ピッチアニメーションを全幅表示に変更（旧 col[11,9] の左カラム55%→全幅、height 590→760）。xTタイムライン/貢献度パネルはピッチ下段の2カラムへ移動。
- 指標説明づけ `describe_features.py` を追加。全特徴量に日本語の内容説明を付与（585列, 未登録0）。横持ち(ヘッダ=名/1行目=内容/2行目=サンプル値)＋縦持ち(1指標1行: No/指標名/カテゴリ/内容/値)の2形式を出力。指標選定レビュー用。
- 全特徴量付与 `enrich_gsa_features.py` を追加。Export_GSA(126列) に feature_catalog 全10カテゴリを付与（自前計算: ①速度②距離③形状④ゾーン⑦個人xT変化⑧ラグ⑩目的変数／dynamic_events・phases結合: ⑤所有局面⑥パス⑨events連動・off-ballラン種別）。1925299のゴールシーンで585列を出力（off-ball run subtype: overlap/cross_receiver等まで実データで取得）。※③ interline(DF-MF-FW by role)のみ未対応（スロットがjersey順のため要role結合）。
- PoC: `batch_skillcorner_to_gsa.py` を追加。SkillCorner Open Data（A-League 10試合）→ GSA入力（Export_GSA 126列形式）を一括生成。各ゴール直前30秒(300frame@10fps)窓を切り出し、player_id↔roster.id 結合・attacking_side で攻撃方向補正・連続lead_to_goalフェーズの畳み込み。10試合=28ゴール窓を出力（ゴール数はスコアと完全一致）。xt_pipeline_22 の関数を再利用。実データはリポジトリ外（C:\skillcorner_opendata, C:\skillcorner_gsa）。

## 2026-06-10

- SoccerNet 連携を整備：取得 `docs/soccernet_download.py`／展開・確認 `docs/soccernet_inspect.py`／GSR→Pitch Log変換 `docs/soccernet_to_pitchlog.py`（Labels-GameState.json の bbox_pitch を長形式トラッキングCSVへ。座標はメートル・中心原点で phase_xt と同系）／xT付与 `docs/soccernet_add_xt.py`（攻撃方向をGK位置から推定し選手別xTを付与）／実データ貢献度レポート `docs/make_realdata_report.py`（選手別 平均xT ランキング＋チーム脅威推移をテンプレ様式pptxで出力）。GSR test split（49クリップ・各30秒/750frame）で検証済み（SNGS-116でxT向き付け＆貢献度ランキング確認）。生成pptxはNDAデータ由来のためコミットせずローカル保持（スクリプトから再生成可）。`DATASETS.md` に NDA 登録手順を追記。
- 提案レポート v2 `docs/Pitch_Log_Report_v2.pptx`（生成: `docs/make_report_v2.py`）を追加。MAZDAテンプレ様式（白/緑/游ゴシック・10x5.625in、原本 `docs/TEMPLATE_PitchLog_MAZDA.pptx`）。「不在だと連鎖が途切れる」画を主役にした攻撃編＋守備編＋主張＋因果の根拠の4枚。今後の pptx はこのテンプレに統一。
- 提案3枚版 `docs/Pitch_Log_Report_3p.pptx`（生成: `docs/make_report_3p.py`）を追加。レポート中心の構成（1=主張「なぜ必要か／因果である」 2=出力レポートのサンプル 3=因果である根拠）。
- 顧客提案スライド `docs/Pitch_Log_Proposal.pptx`（全11枚）を追加。生成スクリプト `docs/make_proposal_pptx.py`。①レーダー→因果6軸→②因果ビュー→連鎖断絶→③シミュレータ→差別化→まとめ の構成。
- `docs/DATASETS.md` を追加。公開トラッキングデータ（映像有無・入手方法）比較表＋ feature_catalog.pptx の10カテゴリ指標が各データで作れるかの対応表。
- モメンタム指標に「xT位置（絶対脅威）」モードを追加。ゴール前の膠着でΔxTが低くなる問題を解消。UIラジオで切替可能。

---

## 2026-06-06

- **TOPハブ `app_top.py` 新規**：v22/v23/v24/取説 への入口を1枚に集約。アプリ比較(VERSIONS)と変更履歴(CHANGELOG)をタブで同居表示。
- アプリ特徴整理 `docs/VERSIONS.md` と変更履歴 `CHANGELOG.md` を追加。push前にCHANGELOG追記する運用ルールを明文化。 (46b2ddb)
- app_22 / app_23 の同一画面下部に「試合全体モメンタム」を埋め込み。共通ロジックを `match_momentum.py` に集約（重複排除）。app_24 はモジュールを使う薄いラッパーに整理。 (73292f7)
- **app_24（v24）新規**：試合全体モメンタムビュー。インタラクティブ Plotly チャート（xT added）、net/正のみ切替、ローリング窓スライダー、ピンチ/チャンス/ゴール自動抽出、ボックス選択で区間抽出＋CSVダウンロード、ズーム解除。`build_match_phases.py` で `match_phases_summary.csv` を生成（cloud用に同梱）。 (e84d023)

## 2026-06-05

- Zone_ID を選手/ボールのホバーと app_22 フレーム表に表示（app_22 / app_23）。 (68eeae8)
- **Zone_ID 座標マッピング追加**：絶対座標 = 深度バンド×100＋幅行（21深度×14幅）。各 subject に `_ZoneID` 列を Export CSV へ。 (e583f69)

## 2026-06-04

- xT ヒートマップの色をチーム規約に統一（Home=青／Away=赤、選手ドット・タイムラインと一致）。 (4f239fa)
- xT ヒートマップの初期表示を「両方（Home＋Away）」に変更。 (49a407b)
- データ生成スクリプト3本を追加し完全再現性を確保（fetch_skillcorner / generate_sample_tracking_22 / create_xt_basemap）。 (5a2a271)
- 生成系CSVを .gitignore に追加。 (5cdec30)
- 不足パイプラインファイルと新PC移行ガイドを追加（xt_pipeline_22 / convert_skillcorner / HANDOFF）。 (7cd6878)

## 2026-05-25 〜 30

- 追加可能カラム カタログ（HTML＋Markdown、500候補）と複数ドキュメントビューアを追加。 (432fcd9, f99b599, 97b9504)
- Δ xT タイムライン＋ゾーン別分析を追加。 (d4786a8)
- GSA結果リーダーの将来仕様メモ（TODO）を追加。 (4142a28)

## 2026-05-23

- 取説アプリ `app_manual.py`（HTML配信ラッパー）と取説HTMLを追加。Dev Container 設定。 (a293edb, 354c6e3, 30bce19, a5f96f2)
- GSA拡張カラム（Delta_xT / dist_ball / speed）を追加。 (ade96e3)
- **app_23（v23）新規**：VAEP＋オフボール＋守備の選手貢献分析。 (87af30a)

## 2026-05-21 〜 22

- 4ステップ シーン取り込みウィザード（フルCSV→切出→変換）とWeb UIデータ取込。 (ae73009, 331204b, 3afe32e)
- ボール半透明・Away xTヒートマップ・選手表示トグル・再生ボタン修正。 (a7a7a68)
- シーン時間窓表示と再切り出し機能。 (af8b72e)
- ゾーン評価モード（ラインブレイクパス検知）。 (1f76ac6)
- ログインを Streamlit 標準 Google 認証に変更。 (f2e67e9, ce75e5f)
- **初版**：Pitch Log 22人 xT ダッシュボード（app_22）。 (c1686cf)
